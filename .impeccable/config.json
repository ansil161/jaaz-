{
  "detector": {
    "ignoreRules": [],
    "ignoreFiles": [],
    "ignoreValues": [
      {
        "rule": "codex-grid-background",
        "value": "grid-blueprint",
        "files": [
          "client/src/index.css"
        ],
        "createdAt": "2026-08-24T08:08:11.319Z",
        "reason": "user supplied reference site finan-portfolio.vercel.app and asked to match its 'How we work' section exact/everything exact; that section's live grid background is bg-[linear-gradient(...)] bg-[size:80px_80px], reproduced here scoped to the single Process/blueprint-themed section only, recolored ink-based to respect this file's own warm-neutral color rule; user confirmed the design is okay after seeing it live"
      },
      {
        "rule": "codex-grid-background",
        "value": "*",
        "files": [
          "client/src/styles/site.css"
        ],
        "createdAt": "2026-08-31T06:58:58.990Z",
        "reason": "Pre-existing committed rule (.grid-blueprint, commit 5e09125), not touched by this session — it surfaced only because a new @custom-variant shifted site.css line numbers by 25. It is the rule's own named carve-out: a literal blueprint surface, used by /about's ProcessTimeline to make that one section read as a drawing rather than a page."
      },
      {
        "rule": "codex-grid-background",
        "value": "grid-blueprint",
        "files": [
          "src/styles/site.css"
        ],
        "createdAt": "2026-08-31T07:27:51.451Z",
        "reason": "Same pre-existing rule already ignored for this file as client/src/styles/site.css; the scanner now emits the path relative to client/ so the existing entry no longer matches. Blueprint surface, the rule's own sanctioned exception: .grid-blueprint is used on exactly one element, ProcessTimeline.jsx:287, a construction-sequence timeline for a company that builds rooms from drawings. Untouched by the footer work; flagged only because editing site.css triggers a full-file scan."
      },
      {
        "rule": "codex-grid-background",
        "value": "*",
        "files": [
          "src/styles/site.css"
        ],
        "createdAt": "2026-09-01T12:09:34.610Z",
        "reason": "Same pre-existing .grid-blueprint rule (committed in 5e09125, used on exactly one element: ProcessTimeline.jsx, a construction-sequence timeline for a company that builds rooms from drawings). It is the rule's own named carve-out for a literal blueprint surface, and it was not touched by this session's work. Re-added because the earlier entry this session was scoped to 'client/src/styles/site.css' while the scanner emits paths relative to client/ ('src/styles/site.css'), so it never matched; and the existing correct-path entry is keyed to value 'grid-blueprint' while the finding now reports no value pair."
      }
    ]
  }
}
